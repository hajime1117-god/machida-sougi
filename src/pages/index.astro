---
import BaseLayout from '../layouts/BaseLayout.astro';
import Cta from '../components/Cta.astro';
import Faq from '../components/Faq.astro';
import Icon from '../components/Icon.astro';
import Source from '../components/Source.astro';
import MachidaMap from '../components/MachidaMap.astro';
import PlanPictogram from '../components/PlanPictogram.astro';
import CostBar from '../components/CostBar.astro';
import { BRAND, PHONE, SITE } from '../config/site';

const title = '町田市の葬儀・家族葬｜費用・斎場・手続きを分かりやすく案内｜町田ことのは葬祭';
const description = '町田市の家族葬・一日葬・火葬式（直葬）の費用と流れ、南多摩斎場など町田市で使える斎場・火葬場、葬祭費5万円の申請まで、出典付きで分かりやすく解説。深夜・早朝のご相談にも対応します。';

const plans = [
  { slug: 'kazokuso', name: '家族葬', price: '528,000', days: '2日', people: '10〜30名', desc: '通夜と告別式を2日間で行う、家族・親族中心の葬儀。', fit: 'ゆっくりお別れしたい方に', key: 'kazokuso' as const },
  { slug: 'ichinichiso', name: '一日葬', price: '396,000', days: '1日', people: '5〜20名', desc: '通夜を省き、告別式と火葬を1日で行う葬儀。', fit: '参列者の負担を軽くしたい方に', key: 'ichinichiso' as const },
  { slug: 'kasoshiki', name: '火葬式・直葬', price: '198,000', days: '火葬のみ', people: '1〜10名', desc: '式を行わず、安置後に火葬場でお別れをする最もシンプルな形式。', fit: '費用を抑えたい方に', key: 'kasoshiki' as const },
];

const costRows = [
  { name: '火葬式・直葬', note: 'お布施別', href: '/plans/kasoshiki/', segs: [{ label: 'プラン料金', value: 198000 }, { label: '式場使用料', value: 0 }, { label: '飲食・返礼品', value: 0 }], total: '約20〜25万円' },
  { name: '一日葬（10名）', note: 'お布施別', href: '/plans/ichinichiso/', segs: [{ label: 'プラン料金', value: 396000 }, { label: '式場使用料', value: 50000 }, { label: '飲食・返礼品', value: 70000 }], total: '約50〜65万円' },
  { name: '家族葬（20名）', note: 'お布施別', href: '/plans/kazokuso/', segs: [{ label: 'プラン料金', value: 528000 }, { label: '式場使用料', value: 50000 }, { label: '飲食・返礼品', value: 240000 }], total: '約75〜95万円' },
];

const faqs = [
  { q: '町田市で亡くなった場合、最初に何をすればいいですか？', a: '<p>まず葬儀社に連絡して、ご遺体の搬送と安置先を決めます。病院で亡くなった場合、数時間以内に霊安室から移動を求められることが多いためです。葬儀の形式や式場は搬送後に落ち着いて決めて構いません。死亡届は亡くなったことを知った日から7日以内に町田市役所市民課などへ提出します。詳しくは<a href="/first-steps/">亡くなってから最初にすること</a>をご覧ください。</p>' },
  { q: '町田市の家族葬はいくらくらいですか？', a: '<p>当サイトの家族葬プランは528,000円（税込）からで、式場使用料・飲食・返礼品・宗教者へのお礼は別途です。南多摩斎場を利用する町田市民は火葬料が無料のため、火葬場の費用を抑えられます。参列人数別の総額例は<a href="/cost/">葬儀費用</a>のページで解説しています。</p>' },
  { q: '町田市で利用できる火葬場はどこですか？', a: '<p>町田市内の公営火葬場は南多摩斎場（町田市上小山田町）の1か所です。町田市を含む組織5市の住民は火葬料が無料です。混雑時は横浜市北部斎場や相模原市営斎場など市外の火葬場も利用できますが、市外料金がかかります。<a href="/halls/crematorium/">町田市の火葬場</a>で料金と使い分けを説明しています。</p>' },
  { q: '葬祭費は受け取れますか？', a: '<p>故人が町田市の国民健康保険または後期高齢者医療制度に加入していた場合、葬儀を行った喪主に葬祭費50,000円が支給されます。申請期限は葬儀の翌日から2年です。会社の健康保険に加入していた場合は、健康保険から埋葬料が支給されます。<a href="/subsidy/">葬祭費・補助金</a>で申請方法を確認できます。</p>' },
  { q: '病院からすぐに葬儀社を決める必要がありますか？', a: '<p>搬送と安置を依頼する葬儀社は急いで決める必要がありますが、葬儀そのものを同じ葬儀社に頼む義務はありません。搬送だけを依頼し、安置後に葬儀の内容と依頼先を検討することができます。当サイトでも搬送のみのご依頼をお受けしています。</p>' },
];

const areas = [
  { href: '/area/machida-eki/', label: '町田駅周辺', note: '原町田・森野・中町・木曽・忠生' },
  { href: '/area/tsurukawa/', label: '鶴川・玉川学園前', note: '能ヶ谷・金井・薬師台・三輪' },
  { href: '/area/naruse/', label: '成瀬・高ヶ坂', note: '成瀬台・南成瀬・西成瀬' },
  { href: '/area/minami-machida/', label: '南町田・つくし野・すずかけ台', note: '鶴間・金森・小川' },
  { href: '/area/tamasakai-aihara/', label: '多摩境・相原・小山', note: '小山ヶ丘・相原町・小山町' },
];
---
<BaseLayout title={title} description={description} pageType="top" fixedCta={{ contactText: '費用を相談' }}>
  <!-- ===== ファーストビュー ===== -->
  <section class="hero" data-region="hero" aria-labelledby="hero-h">
    <div class="container hero__inner">
      <div class="hero__head">
        <p class="hero__eyebrow"><span class="hero__area">東京都町田市</span>の家族葬・一日葬・火葬式</p>
        <h1 id="hero-h">町田市で、<br class="br-pc" />費用と斎場を確かめながら、<br class="br-pc" />自分たちに合う葬儀を選べます。</h1>
      </div>
      <div class="hero__body">
        <p class="hero__lead">{BRAND.name}は、町田市の家族葬・一日葬・火葬式（直葬）を扱う葬儀社サイトです。総額の目安、南多摩斎場など市内外の斎場・火葬場、亡くなった直後の手続きを、町田市の実情に合わせて案内します。</p>
        <ul class="hero__benefits" aria-label="町田市で葬儀をする方に役立つこと">
          <li><span class="hero__benefit-k">24時間365日</span><span class="hero__benefit-v">深夜・早朝の搬送依頼や相談に対応</span></li>
          <li><span class="hero__benefit-k">火葬料 <strong>無料</strong></span><span class="hero__benefit-v">南多摩斎場（町田市民の場合）</span></li>
          <li><span class="hero__benefit-k">葬祭費 <strong>5万円</strong></span><span class="hero__benefit-v">国保・後期高齢者医療から支給</span></li>
        </ul>
        <div class="hero__actions">
          <a href={PHONE.tel} class="btn btn--tel btn--lg" data-cta="tel" data-cta-position="hero"><Icon name="phone" /><span>電話で相談する <span class="num">{PHONE.display}</span></span></a>
          <a href="/cost/" class="btn btn--outline btn--lg" data-cta="cta" data-cta-position="hero"><Icon name="doc" /><span>費用の目安を見る</span></a>
        </div>
        <p class="hero__note">{PHONE.hours}　{PHONE.note}</p>
      </div>
      <div class="hero__visual">
        <figure class="hero__map">
          <MachidaMap variant="simple" title="町田市の概略図。市内の主要駅と、市北西部にある南多摩斎場の位置" />
          <figcaption>町田市内全域に対応。市内唯一の公営火葬場「南多摩斎場」は、町田市民なら火葬料無料。<span class="muted">（図は概略）</span></figcaption>
        </figure>
        <a href="/first-steps/" class="hero__urgent">
          <span class="hero__urgent-label"><Icon name="clock" />病院・施設から連絡を受けた方へ</span>
          <span class="hero__urgent-title">亡くなってから最初にすること（搬送・安置・死亡届）</span>
          <Icon name="arrow" />
        </a>
      </div>
      <ul class="hero__plans" aria-label="対応している葬儀の形式">
        {plans.map((p) => (
          <li>
            <a href={`/plans/${p.slug}/`} class="hero__plan">
              <PlanPictogram plan={p.key} size="sm" />
              <span class="hero__plan-name">{p.name}</span>
              <span class="hero__plan-price"><span class="num">{p.price}</span>円〜</span>
              <span class="hero__plan-days">{p.days}</span>
              <span class="hero__plan-fit">{p.fit}</span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <!-- ===== 状況から選ぶ ===== -->
  <section class="section" aria-labelledby="start-h">
    <div class="container">
      <h2 id="start-h" class="section-title">今の状況に合わせて選ぶ</h2>
      <ul class="starts">
        <li>
          <a href="/first-steps/" class="start">
            <span class="start__label start__label--urgent">今すぐ</span>
            <span class="start__title">すでに亡くなった・危篤と言われた</span>
            <span class="start__desc">搬送先と安置の決め方、死亡届、最初の24時間ですること。</span>
            <Icon name="chevron" />
          </a>
        </li>
        <li>
          <a href="/cost/" class="start">
            <span class="start__label">費用を知りたい</span>
            <span class="start__title">町田市の葬儀費用と総額の目安</span>
            <span class="start__desc">形式別・人数別の総額例、追加になりやすい費用、安く抑える方法。</span>
            <Icon name="chevron" />
          </a>
        </li>
        <li>
          <a href="/plans/" class="start">
            <span class="start__label">事前に備えたい</span>
            <span class="start__title">葬儀の形式を比べて決めておく</span>
            <span class="start__desc">家族葬・一日葬・火葬式の違いと向き不向き。事前相談も受け付けています。</span>
            <Icon name="chevron" />
          </a>
        </li>
      </ul>
    </div>
  </section>

  <!-- ===== プラン ===== -->
  <section class="section section--alt" aria-labelledby="plans-h">
    <div class="container">
      <h2 id="plans-h" class="section-title">町田市の葬儀プラン</h2>
      <p class="section-lead">違いは「通夜と告別式を行うか」だけです。プラン料金には、町田市内の搬送・2日間の安置・棺・ドライアイス・骨壺・火葬場の手続き・当日の進行が含まれます。式場使用料、火葬料（南多摩斎場は町田市民無料）、飲食・返礼品、宗教者へのお礼は別途です。</p>
      <div class="grid grid--3">
        {plans.map((p) => (
          <article class="plan" data-track-view="plan" data-item={p.name} data-region="plan">
            <PlanPictogram plan={p.key} />
            <h3 class="plan__name"><a href={`/plans/${p.slug}/`}>{p.name}</a></h3>
            <p class="plan__price"><span class="num">{p.price}</span><span class="plan__unit">円（税込）〜</span></p>
            <dl class="plan__spec">
              <div><dt>日数</dt><dd>{p.days}</dd></div>
              <div><dt>参列</dt><dd>{p.people}</dd></div>
              <div><dt>式場</dt><dd>{p.key === 'kasoshiki' ? '不要' : '必要'}</dd></div>
            </dl>
            <p class="plan__desc">{p.desc}<strong>{p.fit}</strong>。</p>
            <a href={`/plans/${p.slug}/`} class="plan__link">{p.name}の内容と費用を見る<Icon name="chevron" /></a>
          </article>
        ))}
      </div>
      <p class="note" style="margin-top:1rem">料金は本サイト（検証用）の設定価格です。<a href="/plans/">3つの形式の比較と、自分に合う形式の選び方</a>で、日程・参列人数・向いている方の違いを確認できます。</p>
    </div>
  </section>

  <!-- ===== 費用 ===== -->
  <section class="section" aria-labelledby="cost-h">
    <div class="container">
      <div class="article two-col">
        <div>
          <h2 id="cost-h" class="section-title">町田市で葬儀をすると、総額はいくらかかる？</h2>
          <div class="answer"><p>町田市で南多摩斎場を利用する場合、火葬式で<strong>約25万円</strong>、一日葬で<strong>約50〜65万円</strong>、家族葬（20名）で<strong>約75〜95万円</strong>が総額の目安です。差が出るのは、式場使用料・参列人数に応じた飲食と返礼品・宗教者へのお礼の3つです。</p></div>
          <p class="note">試算の前提：町田市民が南多摩斎場の第二・第三式場（組織市住民50,000円）を使い、火葬料は無料。飲食は1人1食5,000円、返礼品は1人2,000円。宗教者へのお礼は含みません。故人が国保・後期高齢者医療の加入者なら、葬祭費50,000円が後日支給されます。</p>
          <Source items={[{ label: '南多摩斎場 使用料', href: 'https://www.minamitama-saijyo.jp/guide/charge.php' }, { label: '町田市 葬祭費', href: 'https://www.city.machida.tokyo.jp/kurashi/hoken/kokuho/ironnakyuufu/sousai.html' }]} />
        </div>
        <div>
          <CostBar rows={costRows} max={818000} />
          <a href="/cost/" class="next-link"><span><span class="next-link__label">くわしく見る</span>葬儀費用の内訳・人数別の総額例・安く抑える方法</span><Icon name="arrow" /></a>
        </div>
      </div>
      <Cta position="cost" title="費用のことを先に相談したい方へ" lead="「この人数だと総額はいくらか」「今の貯えで収まるか」など、具体的な金額のご相談に無料でお答えします。" telText="電話で費用を相談" contactText="メールで費用を相談" />
    </div>
  </section>

  <!-- ===== 斎場・火葬場 ===== -->
  <section class="section section--alt" aria-labelledby="halls-h">
    <div class="container">
      <div class="article two-col two-col--rev">
        <div>
          <h2 id="halls-h" class="section-title">町田市の斎場・火葬場</h2>
          <div class="answer"><p>町田市内の公営火葬場は<strong>南多摩斎場</strong>（町田市上小山田町）の1か所です。八王子市・町田市・多摩市・稲城市・日野市の5市で運営し、この5市の住民は火葬料が<strong>無料</strong>、市外の方は12歳以上80,000円です。火葬場に併設の式場（第一〜第三式場）で通夜・告別式まで行えます。</p></div>
          <ul class="hall-list">
            <li data-track-view="hall" data-item="南多摩斎場"><span class="hall-list__name">南多摩斎場</span><span class="hall-list__place">町田市上小山田町</span><span class="hall-list__fee"><strong>無料</strong></span><span class="hall-list__fit">市内全域の第一候補</span></li>
            <li data-track-view="hall" data-item="横浜市北部斎場"><span class="hall-list__name">横浜市北部斎場</span><span class="hall-list__place">横浜市緑区長津田町</span><span class="hall-list__fee">50,000円<small>（市外料金）</small></span><span class="hall-list__fit">南町田・つくし野・成瀬</span></li>
            <li data-track-view="hall" data-item="相模原市営斎場"><span class="hall-list__name">相模原市営斎場</span><span class="hall-list__place">相模原市南区古淵</span><span class="hall-list__fee">54,000円<small>（市外料金）</small></span><span class="hall-list__fit">町田駅周辺・木曽・多摩境</span></li>
          </ul>
          <p class="note">火葬料は大人1体・町田市民の場合。公営施設の使用料は非課税。確認日 {SITE.lastReviewed}。南多摩斎場は友引と1月1日〜3日が休場で、混雑期は火葬まで1週間前後待つこともあります。</p>
          <Source items={[{ label: '南多摩斎場 使用料', href: 'https://www.minamitama-saijyo.jp/guide/charge.php' }, { label: '横浜市 斎場使用料', href: 'https://www.city.yokohama.lg.jp/kurashi/sumai-kurashi/saijo/saijo/saijou.html' }, { label: '相模原市営斎場', href: 'https://www.city.sagamihara.kanagawa.jp/kurashi/shisetsu/shikanren/etc/1002725.html' }]} />
        </div>
        <div>
          <figure class="map-figure">
            <MachidaMap variant="crematoriums" title="町田市と周辺の火葬場の位置関係（概略）" />
            <figcaption>◆ 火葬場　● 主な駅　（位置は概略）</figcaption>
          </figure>
          <div class="link-row">
            <a href="/halls/crematorium/" class="next-link"><span><span class="next-link__label">火葬場</span>南多摩斎場の料金・予約・休場日と、混雑時の代替火葬場</span><Icon name="arrow" /></a>
            <a href="/halls/" class="next-link"><span><span class="next-link__label">式場</span>町田市の斎場・葬儀場一覧と、式場の選び方</span><Icon name="arrow" /></a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== 流れ ===== -->
  <section class="section" aria-labelledby="flow-h">
    <div class="container">
      <div class="article">
        <h2 id="flow-h" class="section-title">亡くなってから葬儀までの流れ</h2>
        <div class="answer"><p>町田市では、亡くなった当日に搬送と安置、翌日以降に死亡届と火葬場の予約・打ち合わせ、その後に通夜・告別式・火葬という流れが一般的です。南多摩斎場の空き状況によって、火葬まで<strong>3日〜1週間程度</strong>かかります。</p></div>
        <ol class="stepper stepper--h">
          <li><span class="stepper__t">ご逝去・搬送・安置</span><span class="stepper__d">当日。病院や施設からご自宅または安置施設へ。深夜・早朝でも受付</span></li>
          <li><span class="stepper__t">打ち合わせ・火葬場の予約</span><span class="stepper__d">当日〜翌日。形式・式場・日程を決め、南多摩斎場などの火葬枠を押さえる</span></li>
          <li><span class="stepper__t">死亡届・火葬許可証</span><span class="stepper__d">7日以内。町田市役所市民課（1階104窓口）または市民センターへ。通常は葬儀社が代行</span></li>
          <li><span class="stepper__t">納棺・通夜</span><span class="stepper__d">火葬前日。家族葬の場合。一日葬・火葬式では通夜を行わない</span></li>
          <li><span class="stepper__t">告別式・火葬・収骨</span><span class="stepper__d">火葬当日。火葬は約1〜1.5時間。初七日法要を同日に行うことも</span></li>
        </ol>
        <Source items={[{ label: '町田市 死亡届', href: 'https://www.city.machida.tokyo.jp/kurashi/touroku/todokede/todokede03.html' }]} />
        <div class="link-row">
          <a href="/first-steps/" class="next-link"><span><span class="next-link__label">最初の24時間</span>亡くなってから最初にすること（病院・自宅・施設の場合別）</span><Icon name="arrow" /></a>
          <a href="/flow/" class="next-link"><span><span class="next-link__label">全体の流れ</span>葬儀の流れと手続きの一覧（逝去から法要・各種手続きまで）</span><Icon name="arrow" /></a>
        </div>
      </div>
    </div>
  </section>

  <!-- ===== エリア ===== -->
  <section class="section section--alt" aria-labelledby="area-h">
    <div class="container">
      <h2 id="area-h" class="section-title">町田市内のエリア別案内</h2>
      <p class="section-lead">町田市は南北に長く、お住まいの場所によって近い式場・火葬場が変わります。エリアごとに、最寄りの式場・火葬場・病院と、搬送の目安をまとめています。</p>
      <ul class="area-list">
        {areas.map((a) => (
          <li><a href={a.href}><span class="area-list__label">{a.label}</span><span class="area-list__note">{a.note}</span><Icon name="chevron" /></a></li>
        ))}
      </ul>
      <p class="note"><a href="/machida/">町田市の葬儀ガイド</a>では、市全体の特徴（火葬場が1か所であること、市民葬の現状、費用の目安）をまとめています。</p>
    </div>
  </section>

  <!-- ===== 方針 ===== -->
  <section class="section" aria-labelledby="policy-h">
    <div class="container">
      <h2 id="policy-h" class="section-title">{BRAND.name}の方針</h2>
      <div class="grid grid--3 policies">
        <div class="policy"><h3>追加費用を先に伝える</h3><p>プラン料金に「含まれないもの」を先に説明し、式場使用料・安置日数・返礼品など変動する費用は見積もりで金額を明示します。</p></div>
        <div class="policy"><h3>出典と確認日を示す</h3><p>火葬場の料金や葬祭費などの制度は、町田市・南多摩斎場組合などの公式情報を確認し、出典と最終確認日を掲載しています。</p></div>
        <div class="policy"><h3>急がせない</h3><p>搬送だけのご依頼でも構いません。安置後に葬儀の内容を落ち着いて決めていただき、他社と比較していただいても結構です。</p></div>
      </div>
    </div>
  </section>

  <!-- ===== FAQ ===== -->
  <section class="section section--alt" aria-labelledby="faq-h">
    <div class="container">
      <h2 id="faq-h" class="section-title">町田市の葬儀でよくある質問</h2>
      <Faq items={faqs} group="top" />
      <p><a href="/faq/">すべてのよくある質問を見る</a></p>
      <Cta position="faq" title="分からないことを、そのままお聞かせください" lead="「何から決めればいいか分からない」という段階でのご相談が最も多くあります。町田市の事情に合わせてお答えします。" />
    </div>
  </section>
</BaseLayout>

<style>
  .br-pc { display: none; }
  @media (min-width: 1024px) { .br-pc { display: inline; } }

  /* ---- Hero ---- */
  .hero { background: linear-gradient(180deg, #ffffff 0%, var(--c-bg) 100%); border-bottom: 1px solid var(--c-line-2); padding-block: clamp(1.75rem, 3.5vw, 3.5rem) clamp(1.5rem, 3vw, 2.5rem); }
  .hero__inner { display: grid; gap: 1.25rem; grid-template-areas: 'head' 'plans' 'body' 'visual'; }
  .hero__head { grid-area: head; }
  .hero__body { grid-area: body; }
  .hero__visual { grid-area: visual; }
  .hero__plans { grid-area: plans; }
  .hero__eyebrow { font-size: var(--fs-sm); color: var(--c-ink-2); font-weight: 700; margin: 0 0 0.6em; display: flex; align-items: center; gap: 0.5em; flex-wrap: wrap; }
  .hero__area { display: inline-block; background: var(--c-primary); color: #fff; padding: 0.2em 0.7em; border-radius: 3px; letter-spacing: 0.04em; }
  .hero h1 { font-size: var(--fs-display); line-height: 1.35; margin: 0 0 0.6em; letter-spacing: 0.01em; }
  .hero__lead { color: var(--c-ink-2); max-width: 38em; margin-bottom: 1.1em; font-size: var(--fs-sm); }
  .hero__benefits { list-style: none; padding: 0; margin: 0 0 1.3em; display: grid; gap: 0.45rem; }
  .hero__benefits li { display: grid; grid-template-columns: 9.5em 1fr; gap: 0.6em; align-items: center; font-size: var(--fs-sm); }
  .hero__benefit-k { display: inline-block; background: var(--c-primary-soft); color: var(--c-primary); font-weight: 700; padding: 0.35em 0.6em; border-radius: 3px; text-align: center; line-height: 1.2; }
  .hero__benefit-k strong { font-size: 1.15em; }
  .hero__benefit-v { color: var(--c-ink-2); }
  .hero__actions { display: flex; flex-direction: column; gap: 0.7rem; max-width: 520px; }
  .hero__note { font-size: var(--fs-xs); color: var(--c-ink-3); margin: 0.7em 0 0; }
  .hero__visual { display: grid; gap: 1rem; }
  .hero__map { margin: 0; background: var(--c-surface); border: 1px solid var(--c-line); border-radius: var(--radius-lg); padding: 1rem 1rem 0.8rem; }
  .hero__map figcaption { font-size: var(--fs-xs); color: var(--c-ink-2); margin-top: 0.6em; line-height: 1.6; }
  .hero__urgent { display: grid; grid-template-columns: 1fr auto; align-items: center; gap: 0.2rem 0.75rem; padding: 0.9rem 1.1rem; border: 1px solid #e7c9a6; background: #fff8f0; border-radius: var(--radius-lg); text-decoration: none; color: var(--c-ink); }
  .hero__urgent:hover { border-color: var(--c-accent); }
  .hero__urgent-label { font-size: var(--fs-xs); color: var(--c-accent-2); font-weight: 700; display: inline-flex; align-items: center; gap: 0.3em; }
  .hero__urgent-title { font-weight: 700; grid-row: 2; font-size: var(--fs-sm); }
  .hero__urgent > :global(svg:last-child) { grid-column: 2; grid-row: 1 / span 2; color: var(--c-accent); }
  .hero__plans { list-style: none; padding: 0; margin: 0; display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.5rem; }
  .hero__plans li { margin: 0; }
  .hero__plan { display: grid; grid-template-columns: 1fr; grid-template-areas: 'name' 'price' 'days'; gap: 0.15rem; align-content: start; padding: 0.7rem 0.6rem; background: var(--c-surface); border: 1px solid var(--c-line); border-radius: var(--radius-lg); text-decoration: none; color: var(--c-ink); text-align: center; }
  .hero__plan :global(.pict) { display: none; }
  .hero__plan-fit { display: none; }
  .hero__plan-days { grid-area: days; font-size: var(--fs-xs); color: var(--c-ink-3); }
  .hero__plan:hover { border-color: var(--c-primary-2); }
  .hero__plan :global(.pict) { grid-area: pict; }
  .hero__plan-name { grid-area: name; font-weight: 700; font-size: var(--fs-base); }
  .hero__plan-price { grid-area: price; color: var(--c-primary); font-weight: 700; font-size: var(--fs-xs); white-space: nowrap; }
  .hero__plan-price .num { font-size: 1.2em; }
  @media (min-width: 640px) {
    .hero__actions { flex-direction: row; flex-wrap: wrap; max-width: none; }
    .hero__actions .btn { flex: 0 1 auto; min-width: 240px; }
    .hero__inner { grid-template-areas: 'head' 'body' 'visual' 'plans'; }
    .hero__plans { gap: 1rem; }
    .hero__plan { grid-template-areas: 'pict' 'name' 'price' 'days' 'fit'; gap: 0.35rem; padding: 0.9rem 1rem; text-align: left; }
    .hero__plan :global(.pict) { display: grid; grid-area: pict; }
    .hero__plan-fit { display: block; grid-area: fit; font-size: var(--fs-xs); color: var(--c-ink-3); }
    .hero__plan-name { font-size: var(--fs-lg); margin-top: 0.3rem; }
    .hero__plan-price { font-size: var(--fs-md); }
  }
  @media (min-width: 1024px) {
    .hero__inner { grid-template-columns: minmax(0, 1.15fr) minmax(0, 0.85fr); grid-template-areas: 'head visual' 'body visual' 'plans plans'; grid-template-rows: auto 1fr auto; gap: 0 3.5rem; align-items: start; }
    .hero__plans { margin-top: 2.5rem; }
    .hero__lead { font-size: var(--fs-base); }
    .hero h1 { font-size: 2.3rem; }
    .hero__map { padding: 1.25rem 1.25rem 1rem; }
  }

  /* ---- 状況から選ぶ ---- */
  .starts { list-style: none; padding: 0; margin: 0; display: grid; gap: 0.75rem; }
  .starts li { margin: 0; }
  .start { display: grid; grid-template-columns: 1fr auto; gap: 0.15rem 0.75rem; align-items: center; padding: 1rem 1.1rem; border: 1px solid var(--c-line); border-radius: var(--radius-lg); background: var(--c-surface); text-decoration: none; color: var(--c-ink); }
  .start:hover { border-color: var(--c-primary-2); }
  .start__label { display: inline-block; justify-self: start; font-size: var(--fs-xs); font-weight: 700; color: var(--c-primary); background: var(--c-primary-soft); padding: 0.15em 0.6em; border-radius: 3px; margin-bottom: 0.3em; }
  .start__label--urgent { color: var(--c-accent-2); background: var(--c-accent-soft); }
  .start__title { display: block; font-weight: 700; font-size: var(--fs-md); line-height: 1.4; }
  .start__desc { display: block; font-size: var(--fs-sm); color: var(--c-ink-2); grid-column: 1; }
  .start > :global(svg) { grid-column: 2; grid-row: 1 / span 3; color: var(--c-ink-3); }
  @media (min-width: 768px) { .starts { grid-template-columns: repeat(3, 1fr); gap: 1.25rem; } .start { grid-template-rows: auto auto 1fr; align-content: start; padding: 1.25rem; } }

  /* ---- プラン ---- */
  .plan { background: var(--c-bg); border: 1px solid var(--c-line); border-radius: var(--radius-lg); padding: 1.25rem 1.25rem 1.2rem; display: flex; flex-direction: column; }
  .plan :global(.pict) { max-width: 260px; margin-bottom: 0.9rem; }
  .plan__name { font-size: var(--fs-h3); margin: 0 0 0.3em; }
  .plan__name a { text-decoration: none; color: var(--c-ink); }
  .plan__price { font-size: 1.6rem; font-weight: 700; color: var(--c-primary); margin: 0 0 0.5em; line-height: 1.2; }
  .plan__unit { font-size: var(--fs-sm); color: var(--c-ink-2); font-weight: 400; margin-left: 0.2em; }
  .plan__spec { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.4rem; margin: 0 0 0.9em; }
  .plan__spec div { background: var(--c-surface); border: 1px solid var(--c-line-2); border-radius: var(--radius); padding: 0.4em 0.5em; text-align: center; }
  .plan__spec dt { font-size: 0.7rem; color: var(--c-ink-3); font-weight: 700; }
  .plan__spec dd { margin: 0; font-weight: 700; font-size: var(--fs-sm); }
  .plan__desc { font-size: var(--fs-sm); color: var(--c-ink-2); margin-bottom: 1em; }
  .plan__desc strong { color: var(--c-ink); }
  .plan__link { margin-top: auto; display: inline-flex; align-items: center; gap: 0.2em; font-weight: 700; text-decoration: none; }
  .plan__link:hover { text-decoration: underline; }

  /* ---- 2カラム ---- */
  .two-col { display: grid; gap: 1.5rem; }
  .two-col .section-title { margin-top: 0; }
  @media (min-width: 1024px) { .two-col { grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr); gap: 3rem; align-items: start; } .two-col--rev > :last-child { order: -1; } }
  .map-figure { margin: 0 0 1rem; background: var(--c-bg); border: 1px solid var(--c-line); border-radius: var(--radius-lg); padding: 1rem 1rem 0.7rem; }
  .map-figure figcaption { font-size: var(--fs-xs); color: var(--c-ink-3); margin-top: 0.4em; }
  .link-row { display: grid; gap: 0.25rem; }
  .link-row .next-link { margin: 0.4em 0; }

  /* ---- 火葬場リスト ---- */
  .hall-list { list-style: none; padding: 0; margin: 0 0 1rem; display: grid; gap: 0.5rem; max-width: var(--w-prose); }
  .hall-list li { display: grid; grid-template-columns: 1fr auto; grid-template-areas: 'name fee' 'place fit'; gap: 0.1rem 0.75rem; padding: 0.7rem 0.9rem; background: var(--c-bg); border: 1px solid var(--c-line); border-radius: var(--radius); margin: 0; }
  .hall-list__name { grid-area: name; font-weight: 700; }
  .hall-list__place { grid-area: place; font-size: var(--fs-xs); color: var(--c-ink-3); }
  .hall-list__fee { grid-area: fee; text-align: right; font-weight: 700; color: var(--c-primary); white-space: nowrap; }
  .hall-list__fee small { font-weight: 400; color: var(--c-ink-3); }
  .hall-list__fit { grid-area: fit; text-align: right; font-size: var(--fs-xs); color: var(--c-ink-2); }

  /* ---- エリア ---- */
  .area-list { list-style: none; padding: 0; margin: 0 0 1rem; display: grid; gap: 0.6rem; }
  .area-list li { margin: 0; }
  .area-list a { display: grid; grid-template-columns: 1fr auto; align-items: center; padding: 0.9rem 1.1rem; background: var(--c-bg); border: 1px solid var(--c-line); border-radius: var(--radius); text-decoration: none; color: var(--c-ink); }
  .area-list a:hover { border-color: var(--c-primary-2); }
  .area-list__label { font-weight: 700; }
  .area-list__note { grid-column: 1; font-size: var(--fs-xs); color: var(--c-ink-3); }
  .area-list :global(svg) { grid-column: 2; grid-row: 1 / span 2; color: var(--c-ink-3); }
  @media (min-width: 768px) { .area-list { grid-template-columns: 1fr 1fr; } }
  @media (min-width: 1024px) { .area-list { grid-template-columns: repeat(3, 1fr); } }

  .policy h3 { font-size: var(--fs-md); margin: 0 0 0.5em; padding-left: 0.6em; border-left: 4px solid var(--c-primary-2); }
  .policy p { font-size: var(--fs-sm); color: var(--c-ink-2); margin: 0; }
</style>
